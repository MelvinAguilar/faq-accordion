{
  "name": "faq-accordion",
  "version": "1.0.0",
  "description": "This is an extremely common front-end pattern, so it's an excellent opportunity to get some practice in!",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npx tailwindcss -i ./src/input.css -o ./output.css --watch --minify"
  },
  "author": "MelvinAguilar",
  "license": "ISC",
  "devDependencies": {
    "prettier": "^3.1.0",
    "prettier-plugin-tailwindcss": "^0.5.9",
    "tailwindcss": "^3.3.6"
  }
}
